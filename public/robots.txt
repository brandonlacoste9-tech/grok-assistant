# https://grok-assistant.com
User-agent: *
Allow: /

# App shell only — API is not for indexing
Disallow: /api/
Disallow: /.netlify/

# Only the primary domain — do not submit netlify.app as a second property site map
Sitemap: https://grok-assistant.com/sitemap.xml
